TODO Items
-------------
 * Uses session storage for credientials instead of cookie
 * Use HTTPS
 * Re-save credentials on password change
 * User change own info
 * Add filters to lists
 * Use tokens instead of passing password
